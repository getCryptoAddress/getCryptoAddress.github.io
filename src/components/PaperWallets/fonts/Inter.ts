import getFonts from "@/components/PaperWallets/fonts/utils/getFonts";

const link =
  "https://fonts.googleapis.com/css2?family=Inter:wght@400;700&display=swap";

const fonts = await getFonts(link);
export default fonts;
